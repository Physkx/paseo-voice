# Read secrets directly from the process environment for local development.
PASEO_VOICE_SECRET_PROVIDER=environment

# Required by official OpenAI Realtime (wss://api.openai.com/v1/realtime). Leave unset for mock
# when using that host. Official xAI Grok Voice uses the model/xAI credential path instead
# (XAI_API_KEY, Grok OAuth store, or PASEO_VOICE_SPARK_API_KEY). Credential-free custom
# endpoints can still run live; PASEO_VOICE_MOCK=true always forces mock.
OPENAI_API_KEY=

# Required for Paseo CLI operations.
PASEO_PASSWORD=

# Optional bearer for the summarisation and dictation-cleanup endpoint.
# This dedicated value is also accepted when another provider supplies the OpenAI and Paseo secrets.
# For xAI, either set this or XAI_API_KEY (PASEO_VOICE_SPARK_API_KEY wins when both are set).
PASEO_VOICE_SPARK_API_KEY=
# XAI_API_KEY=

# Optional overrides.
# The API key is sent only to this exact official endpoint. Custom endpoints receive no auth.
# Custom ws:// is accepted only on exact loopback hosts; remote custom endpoints require wss://.
# Realtime host examples (pick one):
# OpenAI:
# PASEO_VOICE_OPENAI_BASE_URL=wss://api.openai.com/v1/realtime
# PASEO_VOICE_OPENAI_MODEL=gpt-realtime-2.1
# PASEO_VOICE_OPENAI_VOICE=marin
# xAI Grok Voice:
PASEO_VOICE_OPENAI_BASE_URL=wss://api.x.ai/v1/realtime
PASEO_VOICE_OPENAI_MODEL=grok-voice-latest
PASEO_VOICE_OPENAI_VOICE=eve
# Plain HTTP model endpoint is accepted on loopback. A Tailscale IPv4 endpoint also needs opt-in.
# Local model example:
# PASEO_VOICE_SPARK_BASE_URL=http://127.0.0.1:1234/v1
# PASEO_VOICE_SPARK_MODEL=qwen3.5-9b-instruct-nvfp4
# xAI chat completions for cleanup/summaries:
PASEO_VOICE_SPARK_BASE_URL=https://api.x.ai/v1
PASEO_VOICE_SPARK_MODEL=grok-4-1-fast-non-reasoning
PASEO_VOICE_ALLOW_INSECURE_TAILSCALE_SPARK=false
